When looking at a group entry, a software entry, and an OS entry we see just a list of hosts.
We want to aggregate some information about them, starting with listening ports.

On the group, software, and os pages, agregate the listening ports of all hosts in those lists.
Present the port numbers in ascending order, with parenthesis like so:

"22 (ssh, 50 hosts)".
