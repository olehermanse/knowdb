Make it so inside the modals for software, classes, and variables, each entry should have a pin button (thumbtack).
Pinning it makes it show up in the host view, always, below the basic information and above these modal buttons.
If a variable or class is not defined on a particular host it should say (not defined) in gray letters and italicized.
For software, it should say (not installed) in gray letters and italicized.

These preferences of what is pinned should be stored in local storage.
