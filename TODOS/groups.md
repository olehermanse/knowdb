Make a groups.json file which holds information about groups of hosts we want to represent.
For now these can be entirely based on string matching the OS and hostname.
There should be a Ubuntu group, Linux group, Windows group, webservers group, production group etc.
Json should be editable by operator later.

Make sure these groups show up as another type of entry in the UI.
Hosts should show which groups they are in and groups should show which host, etc.
